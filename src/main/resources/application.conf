include "local.conf"
ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ dev.barryzeha.ApplicationKt.module ]
    }
}

#JWT
jwt {
    secret="L34rn1ngKt0rWithK0tl1nAndM4st3r1ng$H4ll9Z3h4"
    #This name can be anything, but it's better if it's related to the project.
    realm ="apirest-ktor"
    expiration="3600"
    #This name can be anything, but it's better if it's related to the project.
    issuer="apirest-ktor"
    #This name can be anything, but it's better if it's related to the project.
    audience="apirest-ktor-auth"

}